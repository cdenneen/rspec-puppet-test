---
# Default key/value pairs
custom_facts:
  role: testrole
profile_base::sensu::client_custom:
  custom_properties:
    Team: Common
    Service: common
    Environment: common
lookup_options:
  profile_base::sensu::client_custom:
    merge: deep
